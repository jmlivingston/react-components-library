import { storiesOf } from '@storybook/react'
import React from 'react'
import { storyConfig, storyConfigHelpers } from '../../../../scripts/storybook/storyConfig'
import {{ pascalCase componentName }} from './{{ pascalCase componentName }}'

const componentName = '{{ pascalCase componentName }}'
const category = `${storyConfig.corePrefix}${componentName}`
const config = storyConfigHelpers.getConfig({ componentName })

function {{ pascalCase componentName }}Demo() {
  return <{{ pascalCase componentName }} text="TODO: {{ pascalCase componentName }}" />
}

storiesOf(category, module).add('{{ pascalCase componentName }}', {{ pascalCase componentName }}Demo, config)