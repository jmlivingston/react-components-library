import { storiesOf } from '@storybook/react'
import React from 'react'
import baseConfig from '../../../.storybook/baseConfig'
import packageJson from '../../../package.json'
import {{ pascalCase name }} from './{{ pascalCase name }}'

function {{ pascalCase name }}Demo() {
  return <{{ pascalCase name }} text="TODO: {{ pascalCase name }}" />
}

storiesOf('{{ pascalCase name }}', module).add('{{ pascalCase name }}', {{ pascalCase name }}Demo, {
    ...baseConfig.options,
    info: {
      ...baseConfig.options.info,
      text: `import {{ pascalCase name }} from '${packageJson.name}/cjs/ui/{{ pascalCase name }}'`,
    }
  }
)